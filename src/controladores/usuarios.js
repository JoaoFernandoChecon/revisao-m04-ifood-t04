const cadastrarUsuario = (req, res) => {
  return res.send('Cheguei no controlador')
  //verificarSeEmailExiste(email)
  //const senhaCriptografada = criptografarSenha(senha)
  //cadastrarUsuario({ nome, email, senhaCriptografada })
  //enviarEmail(email)
}

module.exports = {
  cadastrarUsuario,
}
